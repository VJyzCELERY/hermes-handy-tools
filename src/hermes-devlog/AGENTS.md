# Hermes Devlog subproject

Run `uv sync`, then `uv run pytest` and `uv run ruff check .` from this
directory. Runtime behavior is intentionally stdlib-only and must remain
declarative: external actions belong to Hermes.
