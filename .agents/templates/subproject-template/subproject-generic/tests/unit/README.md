# Unit Tests

Place unit tests in this directory.

Naming convention: follow your language/framework standard (e.g., `test_<module>.py` for Python, `<module>.test.ts` for TypeScript, `<module>_test.go` for Go).
