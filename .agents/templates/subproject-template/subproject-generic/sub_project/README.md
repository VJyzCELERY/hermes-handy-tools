# Source Directory

Place your project's source code here.

- Rename this directory to match your subproject name following your language's convention
  (e.g., `snake_case` for Python, `kebab-case` for JavaScript/TypeScript, etc.)
- Organize domain/feature areas into subdirectories (modules)
