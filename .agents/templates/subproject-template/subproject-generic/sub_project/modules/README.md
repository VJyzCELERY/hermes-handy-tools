# Module Directory

This is a placeholder for domain/feature modules.

Replace this folder with your own subdirectories as the project grows
(e.g., `clients/`, `models/`, `tools/`, `services/`, etc.).
